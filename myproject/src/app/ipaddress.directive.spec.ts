import { IpaddressDirective } from './ipaddress.directive';

describe('IpaddressDirective', () => {
  it('should create an instance', () => {
    const directive = new IpaddressDirective();
    expect(directive).toBeTruthy();
  });
});
