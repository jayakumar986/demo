import { MacaddressDirective } from './macaddress.directive';

describe('MacaddressDirective', () => {
  it('should create an instance', () => {
    const directive = new MacaddressDirective();
    expect(directive).toBeTruthy();
  });
});
